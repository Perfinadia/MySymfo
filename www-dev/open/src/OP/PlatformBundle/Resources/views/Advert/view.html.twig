{% extends "OPPlatformBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block ocplatform_body %}

  <h2>{% if advert.image is not null %}
      <img src="{{ advert.image.url }}" alt="{{ advert.image.alt }}">
    {% endif %}
    {{ advert.getTitle() }}</h2>
  <i>Par {{ advert.getAuthor() }}, le {{ advert.getDate()|date('d/m/Y') }}</i>

  <div class="well">
    {{ advert.getContent() }}
  </div>
  {% for app in listApplications %}
    {{ app.getAuthor() }} le {{ app.getDate()|date('d/m/Y') }}.
    <div class="well">
      {{ app.getContent() }}
    </div>
  {% endfor %}
  <p>
    <a href="{{ path('Home_page') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('Edit_page', {'id': advert.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('Delete_page', {'id': advert.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
  </p>

{% endblock %}